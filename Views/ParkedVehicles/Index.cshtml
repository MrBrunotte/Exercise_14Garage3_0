@model Garage3.Models.ViewModels.VehicleTypeViewModel
@inject Garage3.Services.ISelectService service

@{
    ViewData["Title"] = "Index";
}

<h1>Parked Vehicles</h1>

<form asp-action="Filter">
    <div class="input-group mb-3">
        <input type="text" class="form-control" name="SearchString" placeholder="Search Registration Number..." />
        <div class="input-group-append ml-1">
            <select asp-for="VehicleType" asp-items="@Model.VehicleTypes" class="form-control ml-1">
                <option selected="selected" value="">Choose Vehicle Type...</option>
            </select>
        </div>
        <div class="input-group-append ml-1">
            <button type="submit" class="btn btn-primary"><i class="fa fa-search">&emsp;Search</i></button>
        </div>
    </div>
</form>
<partial name="ParkedVehicleListPartial" model="@Model.VehicleList" />

